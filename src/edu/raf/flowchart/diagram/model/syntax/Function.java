package edu.raf.flowchart.diagram.model.syntax;

/**
 * This represents a function (so it has its own context of execution, but the
 * view of global one too)
 * 
 */
public class Function {
	
}
